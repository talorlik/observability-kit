{
  "description": "Seeded rejection fixture: a model provider catalog entry carrying a credential literal instead of a secretref: reference. Pinned to rejection rule fail_if_inline_credential of contracts/ai/MODEL_PROVIDER_ADAPTER_CONTRACT_V1.yaml.",
  "expected_rejection_rule": "fail_if_inline_credential",
  "model_providers": [
    {
      "name": "anthropic-reference",
      "mode": "messages-api-push",
      "vendor_neutral": false,
      "supported_profiles": ["staging", "prod"],
      "required_fields": ["provider", "model_ref", "api_key_ref"],
      "api_key": "inline-literal-credential-SEEDED-FIXTURE"
    }
  ]
}
