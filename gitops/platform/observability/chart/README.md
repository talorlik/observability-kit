# Observability Chart Path

This path is intentionally kept as a compatibility entrypoint for task-linked
automation that references `gitops/platform/observability/chart/`.

The active chart implementation currently lives in
`gitops/charts/platform-core/`.
