# GENERATED by the tenant overlay generator - DO NOT EDIT BY HAND.
# Committed example of generator output; kept under the reserved
# EXAMPLE_TENANT_OVERLAY directory so it never collides with a real
# tenant. Real output path: gitops/overlays/tenants/acme-dev/.
# Source descriptor: contracts/tenancy/samples/VALID_TENANT_BASIC.json
# Contract: contracts/tenancy/TENANT_OVERLAY_GENERATION_CONTRACT_V1.yaml
#
# Flat parameter element merged into the list generator of the tenant
# delivery ApplicationSet. Environment-specific fields (destination
# server, repoURL, targetRevision) live in the ApplicationSet template
# owned by the control plane, never in this element.
element:
  tenant_id: acme-dev
  tier: standard
  isolation_class: shared-partition
  residency_region: eu-central
  residency_pool: shared
  values_path: gitops/overlays/tenants/acme-dev/tenant-values.yaml
