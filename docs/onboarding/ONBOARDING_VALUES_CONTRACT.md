# Onboarding Values Contract

## One-Block Contract

Every service onboards through a single `observability` values block.

Required keys:

- `serviceName`
- `environment`
- `owner`
- `subscriptionMode`

Allowed `subscriptionMode` values:

- `passive`
- `low-touch`
- `instrumentation`
